{
  "$schema": "./node_modules/syncpack/schema.json",
  "source": ["package.json", "apps/*/package.json", "packages/*/package.json"],
  "semverGroups": [
    {
      "label": "Use exact versions for everything (recommended for monorepos)",
      "range": "",
      "dependencies": ["**"],
      "packages": ["**"]
    }
  ]
}
